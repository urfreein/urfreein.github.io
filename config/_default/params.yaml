mainSections:
  - posts

featuredImageField: image

sidebar:
  emoji: 🍥
  subtitle: Tech Blog

article:
  math: true
  readingTime: true
  toc: true

# Widgets 설정 (TOC 표시 위해 필요)
widgets:
  homepage:
    - type: search
    - type: archives
      params:
        limit: 5
    - type: categories
      params:
        limit: 10
    - type: tag-cloud
      params:
        limit: 10
  page:
    - type: toc

comments:
  enabled: false

footer:
  since: 2020
  
defaultImage:
  opengraph:
    enabled: false
